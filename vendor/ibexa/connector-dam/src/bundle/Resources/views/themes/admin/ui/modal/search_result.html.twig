<div class="ibexa-search-result__item">
    <img
        src="{{ asset_path }}"
        alt="{{ alternative_text }}"
        data-source="{{ source_id }}"
        data-id="{{ asset_id }}"
    >
</div>

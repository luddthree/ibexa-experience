<div class="block-targeting {{ block_class }}">
    {{ ibexa_http_cache_tag_relation_location_ids(location.id) }}
    {{ render(controller('ibexa_content:viewAction', {
        'contentId': location.getContentInfo().id,
        'locationId': location.id,
        'viewType': 'embed'
    })) }}
    <style type="text/css">{{ block_style|raw }}</style>
</div>

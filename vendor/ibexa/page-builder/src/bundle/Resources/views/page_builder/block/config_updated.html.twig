<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <meta name="CSRF-Token" content="{{ csrf_token('authenticate') }}" />
    <meta name="SiteAccess" content="{{ app.request.get('siteaccess').name }}" />
    <meta name="BlockConfiguration" content="{{ ibexa_block_value_encode(block_value)}}" />
    <meta name="BlockVisible" content="{{ block_visible|number_format }}" />
</head>
<body>
</body>
</html>

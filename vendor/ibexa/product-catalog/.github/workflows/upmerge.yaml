name: Upmerge (caller)

on:
    workflow_dispatch:

jobs:
    merge:
        uses: ibexa/gh-workflows/.github/workflows/upmerge.yml@main
        secrets:
            robot-token: ${{ secrets.EZROBOT_PAT }}

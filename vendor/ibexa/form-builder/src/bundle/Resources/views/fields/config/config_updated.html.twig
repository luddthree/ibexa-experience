<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <meta name="CSRF-Token" content="{{ csrf_token('authenticate') }}" />
    <meta name="SiteAccess" content="{{ app.request.get('siteaccess').name }}" />
    <meta name="FieldConfiguration" content="{{ ibexa_field_encode(field) }}" />
</head>
<body>
</body>
</html>
